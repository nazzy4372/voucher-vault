module;

import ^.model.*;

struct brand_dto {
    pub_key: byte_array;
    name: text;
}

struct user_dto {
    pub_key: byte_array;
}

struct voucher_type_dto {
    id: text;
    brand: struct<brand>;
    desc: text;
    max_discount: integer;
    max_quantity: integer;
    minted_nft_count: integer;
}

struct nft_voucher_dto {
    id: byte_array;
    owner: struct<user>;
    voucher_type: voucher_type_dto;
    discount: integer;
}
